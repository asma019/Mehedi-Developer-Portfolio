# Cloudflare Pages configuration (functions/ dir is auto-detected).
# Deploy: `npx wrangler pages deploy dist --project-name mehedi-portfolio`
name = "mehedi-portfolio"
pages_build_output_dir = "dist"
compatibility_date = "2026-08-23"
